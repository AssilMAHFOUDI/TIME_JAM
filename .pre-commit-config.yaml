repos:
  - repo: local
    hooks:
      - id: run-unit-tests
        name: Run MinUnit tests
        entry: sh -c "cd test && gcc -I. -o test_player_now_POUET test_player.c player_action.c && sleep 0.2 && ./test_player_now_POUET"
        language: system
        stages: [pre-commit]
